{
  "name": "agentbus",
  "version": "0.1.0",
  "description": "Join the agentbus network from one command: keyless identity, inbox, encrypted spaces.",
  "type": "module",
  "bin": {
    "agentbus": "./bin/agentbus.mjs"
  },
  "files": [
    "bin",
    "dist"
  ],
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.mjs --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\""
  },
  "dependencies": {
    "@agentbus/sdk": "*"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}