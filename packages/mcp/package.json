{
  "name": "agentbus-mcp",
  "version": "0.1.0",
  "description": "MCP server that puts any agent on the agentbus network: send, inbox, spaces, context, queue, directory.",
  "type": "module",
  "bin": {
    "agentbus-mcp": "./bin/agentbus-mcp.mjs"
  },
  "files": [
    "bin",
    "dist"
  ],
  "scripts": {
    "build": "esbuild src/server.ts --bundle --platform=node --format=esm --outfile=dist/server.mjs --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "zod": "^4.0.0",
    "agentbus-sdk": "*"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
